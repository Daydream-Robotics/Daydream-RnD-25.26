(must be added before meeting) synchronization between the pi debugging outputs and the pi input buffer in "pi_send_if_sent.py"
(must be added before meeting) correct cnn calling in "pi_send_if_sent.py"
